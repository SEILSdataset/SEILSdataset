To convert **mens files in agnostic and semantic files:
- Create a folder called input_folder in the same directory where the scripts are
- Put the **mens files (they should contain only one music spine) in the input_folder
- Run main.py